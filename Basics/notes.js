// javascript execution context

// global execution context
// function execution context
// eval execution context

// memory creation phase
// variable object (VO) - stores variables, functions, arguments
// scope chain - current scope + parent scope
// this - refers to the global object (window in browsers)

// code execution phase
// code is executed line by line
// variables are assigned values
// functions are invoked

// call stack - LIFO (last in first out) data structure
// global execution context is pushed to the stack
// function execution context is pushed to the stack when a function is invoked
// function execution context is popped from the stack when a function returns